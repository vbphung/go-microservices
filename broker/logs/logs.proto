syntax = "proto3";

package logs;

option go_package = "/logs";

message Log {
  string name = 1;
  string data = 2;
}

message LogReq { Log logEntry = 1; }

message LogResp { string result = 1; }

service LogService { rpc WriteLog(LogReq) returns (LogResp); }
